import React from 'react';
import {Container, SafeAreaView, Text} from '../../../components/common/common';

const Home = () => {
  return (
    <SafeAreaView>
      <Container>
        <Text text="Home" />
      </Container>
    </SafeAreaView>
  );
};

export default Home;
